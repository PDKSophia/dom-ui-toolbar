/**
 * @description 布局模型
 */
import { ILayoutModel } from '@common/types/model';

const LayoutModel: ILayoutModel = {
  width: 0, // 宽度
  height: 0, // 宽度
  position: 'relative', // 定位
  borderRadius: 0 // 圆角
};

export default LayoutModel;
